import React from 'react';

export default class TracksParams extends React.Component {
  render() {
    return (
      <div>
        Here would be all types of filters for advanced tracks searching
        // https://developers.soundcloud.com/docs/api/reference#tracks -- bottom section
        // select2 + tags
        // slider for duration, though still don't sure about it, when we should update data
        //
      </div>
    );
  }
}
